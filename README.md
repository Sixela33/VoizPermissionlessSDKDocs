# Voiz Markets SDK docs

Mintlify site for `@voiz-digital/markets-sdk`. Source of the published guides — the package README is a shorter version of the same story.

```bash
npm i -g mint
cd sdkdocs
mint dev
```

Preview at `http://localhost:3000`.
