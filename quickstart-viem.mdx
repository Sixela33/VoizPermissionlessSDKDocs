---
title: "Set up with viem"
description: "Same client, no React."
---

You are wiring the client yourself — scripts, backends, or a UI that is not React.

[Install](/install) the SDK and `viem` first. Reads need a public client. Writes need a wallet. A private key is for scripts and backends. A non-React UI should pass the connected wallet. Leave addresses unset unless you deployed your own.

Then you can list existing questions, or create “Will ETH be above $5,000 at year end?” with Yes and No.

## Create a client

```ts
import { VoizMarketsClient, getAddresses } from '@voiz-digital/markets-sdk'
import { createPublicClient, createWalletClient, http } from 'viem'
import { privateKeyToAccount } from 'viem/accounts'
import { baseSepolia } from 'viem/chains'

const chainId = baseSepolia.id
const publicClient = createPublicClient({
  chain: baseSepolia,
  transport: http(),
})

const account = privateKeyToAccount(process.env.PRIVATE_KEY as `0x${string}`)
const walletClient = createWalletClient({
  account,
  chain: baseSepolia,
  transport: http(),
})

const client = new VoizMarketsClient({
  chainId,
  publicClient,
  walletClient,
  // sendCalls,  // AA batcher if you have one
  // addresses: getAddresses(chainId, { /* overrides */ }),
})
```

`walletClient` → one tx per inner call. `sendCalls` → one UserOp for the batch. High-level methods pick whichever you passed.

## Look around

```ts
const { markets } = await client.listMarkets()

const market = client.market(markets[0].address)
await market.summary()
await market.metadata() // copy + images, or null
```

That read is the catalog. Names and answers come back; pictures do not. A market people can trade is [create](/market/create) then [seed](/market/seed), or first liquidity in the same create call.

## Next

- [List markets](/market/list) — browse the catalog
- [Create a market](/market/create) — go live
- [How to think about it](/concepts)
