---
title: "VOIZ Markets SDK"
description: "One client. A living market. Crypto stays underneath."
---

VOIZ turns a question into a live market. This SDK is how you talk to that market: one client, then a market, a pool, or a user. Viem is underneath. You do not assemble calldata.

```ts
client.listMarkets()
client.market(address)   // summary, metadata, seed, resolve, LP
client.pool(outcomeToken) // quote, swap
client.user(owner)        // trades, shares, LP NFTs
```

A **market** is the protocol object. **Metadata** (description, resolution rules, images) is a separate **read** on that same market — it is not mixed into the catalog. You **write** it by passing `metadata` into `createMarket`.

## Install

<CodeGroup>

```bash npm
npm install @voiz-digital/markets-sdk viem
```

```bash pnpm
pnpm add @voiz-digital/markets-sdk viem
```

```bash yarn
yarn add @voiz-digital/markets-sdk viem
```

</CodeGroup>

React apps also want `react`, then import `@voiz-digital/markets-sdk/react`.

## Next

<CardGroup cols={2}>
  <Card title="How to think about it" href="/concepts">
    Client, market, pool, user — and where metadata lives.
  </Card>
  <Card title="How the protocol works" href="/protocol">
    Complete sets, Uniswap LPs, and the rebalancer.
  </Card>
  <Card title="React / Next.js" href="/quickstart-react">
    Wrap the app, grab `useVoiz().client`, list and trade.
  </Card>
  <Card title="Vanilla viem" href="/quickstart-viem">
    Scripts and backends: construct the client yourself.
  </Card>
  <Card title="Create a market" href="/guides/create-market">
    Create, describe, then seed.
  </Card>
</CardGroup>
